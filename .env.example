# ═══════════════════════════════════════════════════════
# VARIÁVEIS DE AMBIENTE - FÓRMULA ENGAJAMENTO
# ═══════════════════════════════════════════════════════
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Preencha os valores necessários
# 3. NUNCA commite o arquivo .env no Git (já está no .gitignore)
# ═══════════════════════════════════════════════════════

# ──────────────────────────────────────────────────────
# BANCO DE DADOS
# ──────────────────────────────────────────────────────
# ✅ Banco de dados SQLite (arquivo local: database.db)
# O arquivo database.db é criado automaticamente e versionado no Git
# Todos os dados (clientes, analytics, configurações) ficam no arquivo
# IMPORTANTE: Não é necessário configurar nenhuma variável de banco!

# ──────────────────────────────────────────────────────
# SEGURANÇA (OBRIGATÓRIO)
# ──────────────────────────────────────────────────────
# Chave secreta para JWT (tokens de autenticação)
# IMPORTANTE: Use uma string aleatória e segura em produção
# Gere uma com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production

# ──────────────────────────────────────────────────────
# CORS (OBRIGATÓRIO)
# ──────────────────────────────────────────────────────
# Origens permitidas para fazer requisições ao backend
# No Replit: Use o domínio do Replit
# Múltiplas origens separadas por vírgula
# Exemplos:
# - Desenvolvimento local: http://localhost:5000
# - Replit: https://seu-projeto.replit.app
# - Produção: https://seudominio.com
ALLOWED_ORIGINS=http://localhost:5000

# ──────────────────────────────────────────────────────
# CRIAÇÃO DE ADMINS (OPCIONAL)
# ──────────────────────────────────────────────────────
# Permite criação de admins via API (DESABILITE EM PRODUÇÃO)
# Valores: true ou false
# IMPORTANTE: Mantenha como false em produção por segurança
# Use o script npm run create-admin para criar admins
ALLOW_ADMIN_CREATION=false

# ──────────────────────────────────────────────────────
# SERVIDOR (OPCIONAL)
# ──────────────────────────────────────────────────────
# Porta do servidor backend
# Padrão: 3001 (não mude a menos que necessário)
PORT=3001

# Ambiente de execução
# Valores: development, production
NODE_ENV=development

# ──────────────────────────────────────────────────────
# URL DA API (IMPORTANTE PARA VERCEL)
# ──────────────────────────────────────────────────────
# URL base da API backend
# 
# DESENVOLVIMENTO (Replit): Deixe em branco (usa proxy do Vite)
# PRODUÇÃO (Vercel): Configure com a URL do backend
# 
# Exemplos:
# - Desenvolvimento: (deixe em branco)
# - Produção Vercel: https://seu-backend.railway.app
# - Produção Vercel: https://seu-backend.render.com
# - Backend no Replit: https://seu-projeto-backend.replit.app
VITE_API_BASE_URL=
